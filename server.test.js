import { pipelineBareFunction } from "@babel/types";

describe("Web Calc Functionality", async () => {
const response = await post('2+2')    () => {
      expect(new Calculator(2).add(3)).toBe(5);
    };
});

describe("POST /'2+2", function() {
    it('returns 4', function(done) {
      request(app)  //but this uses express ;-;
        .post("/'2+2'")
        .set('Accept', 'application/json')
        .expect('Content-Type', text/plain)
        .expect(200)
        .end(function(err, res) {
          if (err) return done(err);
          done();
        });
    });
  });
